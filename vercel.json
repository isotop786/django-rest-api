{
    "builds": [{
        "src": "admin_app/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "admin_app/wsgi.py"
        }
    ]
}